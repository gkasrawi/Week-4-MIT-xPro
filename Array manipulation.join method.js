Array manipulation.join method 
const words = ['I', 'love', 'pizza']  
 
// The .join method Sums all the elements of an array into a string, differentiated by the specified separator string.
const str = words.join(' '); // will compily array elements into a string; proving a separator index. 
console.log(str); //[ 'Hey', 'I', 'love', 'pizza']  method expression
